---
version: "2.0.0"
name: git-helper
description: "Git命令生成、冲突解决、工作流(GitFlow/Trunk)、.gitignore生成、commit message、撤销操作。Git command generator, conflict resolver, workflow guide, .gitignore generator, commit message writer, undo operations. Use when you need git helper capabilities. Triggers on: git helper."
---
# git-helper

Git命令生成、冲突解决、工作流(GitFlow/Trunk)、.gitignore生成、commit message、撤销操作。Git command generator, conflict resolver, workflow guide, .gitignore generator, commit message writer, undo operations.

## 常见问题

**Q: 这个工具适合谁用？**
A: 任何需要git-helper的人，无论是个人还是企业用户。

**Q: 输出格式是什么？**
A: 主要输出Markdown格式，方便复制和编辑。

## 命令列表

| 命令 | 功能 |
|------|------|
| `command` | command |
| `conflict` | conflict |
| `a` | a |
| `b` | b |
| `c` | c |
| `workflow` | workflow |
| `ignore` | ignore |
| `message` | message |


## 专业建议

- ```bash
- git init                    # 初始化仓库
- git clone <url>             # 克隆仓库
- git add <file>              # 暂存文件
- git add .                   # 暂存所有修改

---
*git-helper by BytesAgain*
---
💬 Feedback & Feature Requests: https://bytesagain.com/feedback
Powered by BytesAgain | bytesagain.com

## Commands

| Command | Description |
|---------|-------------|
| `git-helper help` | Show usage info |
| `git-helper run` | Run main task |
