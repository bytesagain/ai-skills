---
name: "splice"
version: "1.0.0"
description: "Array and sequence splicing reference — insert, remove, and replace elements in arrays, strings, and data streams. Use when understanding splice operations, implementing in-place mutations, or comparing splice vs slice semantics."
author: "BytesAgain"
homepage: "https://bytesagain.com"
source: "https://github.com/bytesagain/ai-skills"
tags: [splice, array, mutation, insert, remove, data-structure]
category: "atomic"
---

# Splice — Array & Sequence Splicing Reference

Quick-reference skill for splice operations across languages — insert, remove, replace elements in-place.

## When to Use

- Understanding JavaScript Array.prototype.splice behavior
- Implementing splice in other languages (Python, Rust, Go)
- Comparing splice vs slice (mutating vs non-mutating)
- Splicing binary data, strings, or linked lists
- Optimizing bulk insert/remove operations

## Commands

### `intro`

```bash
scripts/script.sh intro
```

What splicing means, how it differs from slicing, and core semantics.

### `javascript`

```bash
scripts/script.sh javascript
```

JavaScript Array.splice — complete API, return values, edge cases.

### `python`

```bash
scripts/script.sh python
```

Python slice assignment — the Pythonic way to splice lists.

### `languages`

```bash
scripts/script.sh languages
```

Splice equivalents in Rust, Go, Java, C++, and Ruby.

### `strings`

```bash
scripts/script.sh strings
```

String splicing — insert, remove, replace substrings across languages.

### `binary`

```bash
scripts/script.sh binary
```

Splicing binary data, buffers, and byte arrays.

### `performance`

```bash
scripts/script.sh performance
```

Time complexity, memory implications, and optimization strategies.

### `patterns`

```bash
scripts/script.sh patterns
```

Common splice patterns: dedup, interleave, chunk insert, circular buffer.

### `help`

```bash
scripts/script.sh help
```

### `version`

```bash
scripts/script.sh version
```

---

*Powered by BytesAgain | bytesagain.com | hello@bytesagain.com*
