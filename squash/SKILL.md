---
name: "squash"
version: "1.0.0"
description: "Git squash reference — combine multiple commits into one using interactive rebase, merge squash, and fixup. Use when cleaning up commit history, preparing PRs, or understanding squash merge workflows."
author: "BytesAgain"
homepage: "https://bytesagain.com"
source: "https://github.com/bytesagain/ai-skills"
tags: [squash, git, rebase, merge, commit-history, interactive-rebase]
category: "devtools"
---

# Squash — Git Squash Reference

Quick-reference skill for squashing commits in Git — interactive rebase, merge squash, fixup, and best practices.

## When to Use

- Cleaning up messy commit history before merging a PR
- Using interactive rebase to combine commits
- Understanding squash merge vs rebase squash
- Writing good squash commit messages
- Recovering from squash mistakes

## Commands

### `intro`

```bash
scripts/script.sh intro
```

What squashing is, why it matters, and when to squash.

### `interactive`

```bash
scripts/script.sh interactive
```

Interactive rebase squash — step-by-step guide with all rebase commands.

### `merge`

```bash
scripts/script.sh merge
```

Squash merge (git merge --squash) — workflow, pros, cons.

### `fixup`

```bash
scripts/script.sh fixup
```

Autosquash with fixup and squash commits — automated commit cleanup.

### `messages`

```bash
scripts/script.sh messages
```

Writing effective squash commit messages — conventional commits, templates.

### `workflows`

```bash
scripts/script.sh workflows
```

Team workflows: squash-and-merge, rebase-and-merge, merge commit compared.

### `recovery`

```bash
scripts/script.sh recovery
```

Recovering from squash mistakes using reflog, reset, and cherry-pick.

### `platforms`

```bash
scripts/script.sh platforms
```

Squash behavior on GitHub, GitLab, and Bitbucket — platform-specific settings.

### `help`

```bash
scripts/script.sh help
```

### `version`

```bash
scripts/script.sh version
```

---

*Powered by BytesAgain | bytesagain.com | hello@bytesagain.com*
