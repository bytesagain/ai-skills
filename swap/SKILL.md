---
name: "swap"
version: "1.0.0"
description: "Swap operations reference — exchanging values, memory pages, and partitions. Use when understanding variable swapping algorithms, OS swap space management, or atomic exchange operations."
author: "BytesAgain"
homepage: "https://bytesagain.com"
source: "https://github.com/bytesagain/ai-skills"
tags: [swap, exchange, memory, os, algorithm, atomic]
category: "atomic"
---

# Swap — Value & Memory Swap Reference

Quick-reference skill for swap operations — from variable exchange to OS virtual memory swap.

## When to Use

- Swapping variables without temp (XOR, destructuring, std::swap)
- Understanding Linux swap space and swappiness tuning
- Implementing atomic compare-and-swap (CAS) operations
- Swap-based sorting algorithms
- Memory page swapping and thrashing diagnosis

## Commands

### `intro`

```bash
scripts/script.sh intro
```

Overview of swap — variable exchange, memory swap, and atomic operations.

### `variables`

```bash
scripts/script.sh variables
```

Variable swapping techniques across languages — temp, XOR, destructuring.

### `os`

```bash
scripts/script.sh os
```

OS swap space — Linux swap partition, swapfile, swappiness, performance.

### `atomic`

```bash
scripts/script.sh atomic
```

Atomic swap operations — CAS, compare-and-exchange, lock-free patterns.

### `sorting`

```bash
scripts/script.sh sorting
```

Swap in sorting algorithms — bubble, selection, quicksort partition.

### `containers`

```bash
scripts/script.sh containers
```

Container swap — std::swap, Vec::swap, buffer swapping, double buffering.

### `diagnosis`

```bash
scripts/script.sh diagnosis
```

Diagnosing swap issues — thrashing, OOM, monitoring tools.

### `financial`

```bash
scripts/script.sh financial
```

Financial swaps — interest rate swaps, currency swaps, credit default swaps.

### `help`

```bash
scripts/script.sh help
```

### `version`

```bash
scripts/script.sh version
```

---

*Powered by BytesAgain | bytesagain.com | hello@bytesagain.com*
