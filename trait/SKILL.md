---
name: "trait"
version: "1.0.0"
description: "Trait and interface reference — shared behavior contracts in Rust, PHP, Scala, and Kotlin. Use when designing polymorphic APIs, understanding trait vs interface vs abstract class, or implementing trait objects and default methods."
author: "BytesAgain"
homepage: "https://bytesagain.com"
source: "https://github.com/bytesagain/ai-skills"
tags: [trait, interface, polymorphism, rust, php, abstract, generics]
category: "devtools"
---

# Trait — Trait & Interface Reference

Quick-reference skill for traits — shared behavior contracts, default implementations, and polymorphism patterns.

## When to Use

- Defining shared behavior across unrelated types
- Understanding trait vs interface vs abstract class
- Implementing Rust traits with generics and trait objects
- Using PHP traits for horizontal code reuse
- Designing extensible APIs with trait bounds

## Commands

### `intro`

```bash
scripts/script.sh intro
```

What traits are, trait vs interface vs abstract class, the diamond problem.

### `rust`

```bash
scripts/script.sh rust
```

Rust traits — definition, impl, default methods, trait bounds, dyn dispatch.

### `php`

```bash
scripts/script.sh php
```

PHP traits — horizontal reuse, conflict resolution, abstract methods.

### `scala`

```bash
scripts/script.sh scala
```

Scala traits — mixins, linearization, stackable modifications.

### `kotlin`

```bash
scripts/script.sh kotlin
```

Kotlin interfaces with default methods and delegation.

### `patterns`

```bash
scripts/script.sh patterns
```

Design patterns with traits: extension, marker, sealed, newtype.

### `generics`

```bash
scripts/script.sh generics
```

Trait bounds, associated types, and generic programming with traits.

### `comparison`

```bash
scripts/script.sh comparison
```

Trait features compared across Rust, PHP, Scala, Kotlin, Swift, Haskell.

### `help`

```bash
scripts/script.sh help
```

### `version`

```bash
scripts/script.sh version
```

---

*Powered by BytesAgain | bytesagain.com | hello@bytesagain.com*
