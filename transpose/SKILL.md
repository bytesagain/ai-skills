---
name: "transpose"
version: "1.0.0"
description: "Matrix and data transposition reference — row-column swapping for matrices, database pivots, and musical key changes. Use when transposing arrays, pivoting data tables, or understanding transpose in linear algebra."
author: "BytesAgain"
homepage: "https://bytesagain.com"
source: "https://github.com/bytesagain/ai-skills"
tags: [transpose, matrix, pivot, linear-algebra, data-transform, array]
category: "atomic"
---

# Transpose — Matrix & Data Transposition Reference

Quick-reference skill for transposing data — matrices, arrays, database tables, and musical keys.

## When to Use

- Transposing a 2D array or matrix (rows ↔ columns)
- Understanding transpose properties in linear algebra
- Pivoting data tables (SQL PIVOT, pandas pivot_table)
- Implementing cache-efficient transpose for large matrices
- Transposing musical keys and chord progressions

## Commands

### `intro`

```bash
scripts/script.sh intro
```

What transposition means — matrices, data, music — core concept.

### `matrix`

```bash
scripts/script.sh matrix
```

Matrix transpose — notation, properties, symmetric matrices, conjugate transpose.

### `code`

```bash
scripts/script.sh code
```

Transpose implementations in Python, JavaScript, Rust, NumPy, and SQL.

### `inplace`

```bash
scripts/script.sh inplace
```

In-place transpose algorithms — square and non-square matrices.

### `performance`

```bash
scripts/script.sh performance
```

Cache-efficient transpose — blocking, SIMD, parallel strategies.

### `data`

```bash
scripts/script.sh data
```

Data transposition — pivot tables, wide vs long format, reshaping.

### `music`

```bash
scripts/script.sh music
```

Musical transposition — key changes, intervals, chord transposition.

### `applications`

```bash
scripts/script.sh applications
```

Transpose applications — image rotation, graph adjacency, signal processing.

### `help`

```bash
scripts/script.sh help
```

### `version`

```bash
scripts/script.sh version
```

---

*Powered by BytesAgain | bytesagain.com | hello@bytesagain.com*
