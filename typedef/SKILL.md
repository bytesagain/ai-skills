---
name: "typedef"
version: "1.0.0"
description: "Type definition reference — creating type aliases, newtypes, and custom types in C, C++, Rust, Go, and TypeScript. Use when designing type systems, understanding typedef vs using, or creating domain-specific types."
author: "BytesAgain"
homepage: "https://bytesagain.com"
source: "https://github.com/bytesagain/ai-skills"
tags: [typedef, type-alias, newtype, type-system, generic, declaration]
category: "devtools"
---

# Typedef — Type Definition Reference

Quick-reference skill for type definitions — aliases, newtypes, and custom types across languages.

## When to Use

- Creating readable type aliases for complex types
- Understanding C typedef vs C++ using
- Implementing newtype pattern for type safety
- Designing TypeScript utility types and mapped types
- Comparing type definition approaches across languages

## Commands

### `intro`

```bash
scripts/script.sh intro
```

What type definitions are, alias vs newtype, why custom types matter.

### `clang`

```bash
scripts/script.sh clang
```

C typedef — structs, function pointers, opaque types, platform types.

### `cpp`

```bash
scripts/script.sh cpp
```

C++ using declarations — template aliases, comparison with typedef.

### `rust`

```bash
scripts/script.sh rust
```

Rust type aliases and newtype pattern — type safety, From/Into, Deref.

### `typescript`

```bash
scripts/script.sh typescript
```

TypeScript type aliases, interfaces, utility types, mapped types, branded types.

### `golang`

```bash
scripts/script.sh golang
```

Go type definitions vs type aliases — methods, compatibility, embedding.

### `patterns`

```bash
scripts/script.sh patterns
```

Type definition patterns: branded, phantom, opaque, discriminated unions.

### `comparison`

```bash
scripts/script.sh comparison
```

Type definition features compared across C, C++, Rust, Go, TS, Haskell.

### `help`

```bash
scripts/script.sh help
```

### `version`

```bash
scripts/script.sh version
```

---

*Powered by BytesAgain | bytesagain.com | hello@bytesagain.com*
